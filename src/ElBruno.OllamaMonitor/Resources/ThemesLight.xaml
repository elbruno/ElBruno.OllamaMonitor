<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Light Theme Brushes -->
    <SolidColorBrush x:Key="ThemeBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ThemeForegroundBrush" Color="#1A1A1A"/>
    <SolidColorBrush x:Key="ThemeAccentBrush" Color="#007ACC"/>
    <SolidColorBrush x:Key="ThemePanelBrush" Color="#F3F4F6"/>
    <SolidColorBrush x:Key="ThemeBorderBrush" Color="#E5E7EB"/>
    <SolidColorBrush x:Key="ThemeSecondaryForegroundBrush" Color="#4B5563"/>

    <!-- Button Styles -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#007ACC"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#007ACC"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="14,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#0098FF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- GroupBox Styles -->
    <Style TargetType="{x:Type GroupBox}">
        <Setter Property="Background" Value="#F9FAFB"/>
        <Setter Property="BorderBrush" Value="#E5E7EB"/>
        <Setter Property="Foreground" Value="#1A1A1A"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Window Styles -->
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="Foreground" Value="#1A1A1A"/>
    </Style>

</ResourceDictionary>
